## OpenAI-compatible endpoint config (used by `adk/widget_creator/agent.py`)
#
# Copy to `.env` in the repo root (if you use dotenv), then edit values as needed.
# Include `/v1` at the end of the base URL.
OPENAI_BASE_URL="http://localhost:8000/v1"
OPENAI_API_KEY="local-dev"
OPENAI_MODEL="openai/gpt-4o-mini"


8gNWDW5w1WouV6dFDT1eYqu9kQ8dioew

